package cmd

const (
	signedJwt      = `jwt`
	token          = `token`
	privateKeyPath = `private-key-path`
	appId          = `app-id`
	installId      = `install-id`
	page           = `page`
	perPage        = `per-page`
)
